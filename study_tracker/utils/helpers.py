# utils/helpers.py
# General-purpose helper/utility functions shared across the project.
# Responsibilities:
#   - format_date(date_str): parse and standardize date strings to YYYY-MM-DD
#   - get_today(): return today's date as a YYYY-MM-DD formatted string
#   - clear_screen(): clear the terminal for a cleaner CLI experience
#   - print_separator(): print a dividing line (e.g., "---") for visual clarity in menus
#   - validate_score(score): check that a value is a number between 0 and 100
#   - validate_duration(duration): check that duration is a positive number
